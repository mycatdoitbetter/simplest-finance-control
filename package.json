{
  "name": "simplest-finance-control",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "dev": "concurrently \"nodemon --watch server server/index.js\" \"browser-sync start -c bs-config.js\"",
    "seed": "node server/seed.js"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "concurrently": "^9.0.1",
    "nodemon": "^3.1.0"
  }
}
